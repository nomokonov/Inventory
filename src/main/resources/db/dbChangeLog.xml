<?xml version="1.0" encoding="utf-8" ?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">
    <changeSet author="Nomokonov Vladimir" id="1">
        <comment>
            Create TABLES ;
        </comment>
        <sql>

            CREATE TABLE public.fromexcel
            (
                id integer NOT NULL DEFAULT nextval('fromexcel_id_seq'::regclass),
                name character varying COLLATE pg_catalog."default",
                invnumber character varying COLLATE pg_catalog."default",
                quantity integer,
                unit character varying COLLATE pg_catalog."default",
                price numeric,
                typetmc character varying COLLATE pg_catalog."default",
                typeutilize character varying COLLATE pg_catalog."default",
                codegroup character varying COLLATE pg_catalog."default",
                namegroup character varying COLLATE pg_catalog."default",
                mol character varying COLLATE pg_catalog."default",
                codedepartment character varying COLLATE pg_catalog."default",
                namedepartment character varying COLLATE pg_catalog."default",
                locations character varying COLLATE pg_catalog."default",
                nonsystem character varying COLLATE pg_catalog."default",
                datecreation date,
                datepostings date,
                dateexploit date,
                dateclosin date,
                oldinvnumber character varying COLLATE pg_catalog."default",
                CONSTRAINT fromexcel_pkey PRIMARY KEY (id)
            )

        </sql>
        <rollback>
            DROP TABLE fromexcel;
        </rollback>
    </changeSet>
    <changeSet author="Nomokonov Vladimir" id="2">
        <comment>
            Create TABLE for movings ;
        </comment>
        <sql>

            CREATE TABLE public.movings
            (
                id integer NOT NULL DEFAULT nextval('movings_id_seq'::regclass),
                fromexcelid integer NOT NULL DEFAULT nextval('movings_fromexcelid_seq'::regclass),
                mol character varying COLLATE pg_catalog."default",
                codedepartment character varying COLLATE pg_catalog."default",
                namedepartment character varying COLLATE pg_catalog."default",
                CONSTRAINT movings_pkey PRIMARY KEY (id),
                CONSTRAINT movings_fromexcelid_fkey FOREIGN KEY (fromexcelid)
                REFERENCES public.fromexcel (id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION,
                CONSTRAINT movings_fromexcelid_fkey1 FOREIGN KEY (fromexcelid)
                REFERENCES public.fromexcel (id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
            )

        </sql>
        <rollback>
            DROP TABLE fromexcel;
        </rollback>
    </changeSet>
</databaseChangeLog>
